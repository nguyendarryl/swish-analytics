-- CREATE DATABASE swish;

CREATE TABLE bet (
  component_cnt_bet INTEGER,
  selection VARCHAR(255),
  line NUMERIC,
  is_alternate BOOLEAN,
  is_inplay BOOLEAN,
  is_active BOOLEAN,
  is_cashout VARCHAR(255),
  currency VARCHAR(255),
  book_risk_component NUMERIC,
  book_profit_gross_component NUMERIC,
  client_id INTEGER,
  country VARCHAR(255),
  state VARCHAR(2),
  accepted_datetime_utc timestamptz,
  component_price NUMERIC,
  component_prob NUMERIC,
  component_num_bet INTEGER,
  bet_type_id INTEGER,
  bet_type VARCHAR(255),
  book_risk NUMERIC,
  parlay_type_id INTEGER,
  book_profit_gross NUMERIC,
  parlay_type VARCHAR(255),
  bet_price NUMERIC,
  bet_prob NUMERIC,
  country_id INTEGER,
  state_id INTEGER,
  currency_id INTEGER,
  sport_id INTEGER,
  sport VARCHAR(255),
  season INTEGER,
  date DATE,
  datetime_utc timestamptz,
  event_id INTEGER,
  event_status_id INTEGER,
  event_type_id INTEGER,
  stat_type_id INTEGER,
  stat_type VARCHAR(255),
  product_id INTEGER,
  product VARCHAR(255),
  team_id INTEGER,
  opp_id INTEGER,
  team_abbr VARCHAR(255),
  opp_abbr VARCHAR(255),
  home BOOLEAN,
  player_id INTEGER,
  player_name VARCHAR(255),
  pos_id INTEGER,
  pos_abbr VARCHAR(255),
  game_played BOOLEAN,
  game_started BOOLEAN,
  actual NUMERIC,
  accepted_min_before_start NUMERIC,
  settled_date DATE,
  market_duration_id INTEGER,
  market_duration_value VARCHAR(255),
  market_duration_type VARCHAR(255),
  bet_id_swish VARCHAR(255),
  component_id_swish VARCHAR(255),
  line_at_bet VARCHAR(255),
  prob_norm_at_bet VARCHAR(255),
  proj_at_bet VARCHAR(255),
  market_suspended_id VARCHAR(255),
  market_suspended_description VARCHAR(255),
  gamestate_id NUMERIC,
  gamestate VARCHAR(255),
  score NUMERIC,
  opp_score NUMERIC,
  period NUMERIC,
  usage_at_bet NUMERIC,
  actual_at_bet NUMERIC,
  is_in_game NUMERIC,
  event_time_id INTEGER,
  event_time_remaining INTEGER,
  usage_id INTEGER,
  orig_proj_at_bet NUMERIC,
  line_diff_at_bet NUMERIC,
  prob_diff_at_bet NUMERIC,
  received_date DATE,
  client_name VARCHAR(255)
);
